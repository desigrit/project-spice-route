<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- Retain WinUI's native pointer, selection, keyboard focus and high-contrast states. -->
  <Style x:Key="SpiceReviewRowStyle" TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
    <Setter Property="Padding" Value="0" />
    <Setter Property="Margin" Value="0" />
    <Setter Property="MinWidth" Value="0" />
    <Setter Property="MinHeight" Value="56" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="UseSystemFocusVisuals" Value="True" />
  </Style>
  <Style x:Key="SpiceReviewListStyle" TargetType="ListView">
    <Setter Property="Padding" Value="0" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
    <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled" />
    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
  </Style>
  <Style x:Key="SpiceReviewSidebarStyle" TargetType="Border">
    <Setter Property="BorderBrush" Value="{ThemeResource SpiceLine}" />
  </Style>
  <DataTemplate x:Key="SpiceReviewFileTemplate">
    <Grid ColumnSpacing="12" Padding="12,8" Height="56" ToolTipService.ToolTip="{Binding Detail}">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="76" />
        <ColumnDefinition Width="70" />
      </Grid.ColumnDefinitions>
      <StackPanel Spacing="2" VerticalAlignment="Center">
        <TextBlock x:Name="ReviewFileName" Text="{Binding Label}" FontWeight="SemiBold" FontSize="13" TextTrimming="CharacterEllipsis" />
        <TextBlock Text="{Binding Detail}" FontSize="12" TextTrimming="CharacterEllipsis" />
      </StackPanel>
      <TextBlock x:Name="ReviewFileChange" Grid.Column="1" Text="{Binding ActionLabel}" FontSize="12" VerticalAlignment="Center" />
      <TextBlock x:Name="ReviewFileSize" Grid.Column="2" Text="{Binding Size}" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center" />
    </Grid>
  </DataTemplate>
  <DataTemplate x:Key="SpiceReviewProjectTemplate">
    <Grid Height="56" Padding="12,8" ToolTipService.ToolTip="{Binding AccessibleName}" AutomationProperties.Name="{Binding AccessibleName}">
      <StackPanel Spacing="2" VerticalAlignment="Center">
        <TextBlock x:Name="ReviewProjectName" Text="{Binding Name}" FontSize="13" TextTrimming="CharacterEllipsis" />
        <TextBlock x:Name="ReviewProjectSummary" Text="{Binding Summary}" FontSize="12" TextTrimming="CharacterEllipsis" />
      </StackPanel>
    </Grid>
  </DataTemplate>
  <Style x:Key="SpiceSyncRowStyle" TargetType="ListViewItem" BasedOn="{StaticResource SpiceReviewRowStyle}">
    <Setter Property="MinHeight" Value="49" />
    <Setter Property="AutomationProperties.Name" Value="{Binding AccessibleName}" />
  </Style>
  <DataTemplate x:Key="SpiceSyncProjectTemplate">
    <Grid ColumnSpacing="12" Padding="12,0" Height="49" BorderBrush="{ThemeResource SpiceLine}" BorderThickness="0,0,0,1">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="137"/><ColumnDefinition Width="70"/></Grid.ColumnDefinitions>
      <Grid ColumnSpacing="9"><Grid.ColumnDefinitions><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <FontIcon Glyph="&#xE8B7;" FontFamily="Segoe Fluent Icons" FontSize="15" Foreground="{ThemeResource SpiceAccent}"/>
        <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="13" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding Name}"/>
      </Grid>
      <TextBlock Grid.Column="1" Text="{Binding ModeLabel}" FontSize="12" Foreground="{ThemeResource SpiceTextSecondary}" VerticalAlignment="Center"/>
      <TextBlock Grid.Column="2" Text="{Binding Size}" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Right"/>
    </Grid>
  </DataTemplate>
  <DataTemplate x:Key="SpiceSyncChatTemplate">
    <Grid ColumnSpacing="12" Padding="12,4" MinHeight="60" BorderBrush="{ThemeResource SpiceLine}" BorderThickness="0,0,0,1">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="70"/></Grid.ColumnDefinitions>
      <CheckBox IsChecked="{Binding Included, Mode=TwoWay}" IsEnabled="{Binding Allowed}" HorizontalContentAlignment="Stretch" AutomationProperties.Name="{Binding Name}">
        <StackPanel Spacing="3"><TextBlock Text="{Binding Name}" FontSize="13" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding Detail}" FontSize="12" Foreground="{ThemeResource SpiceTextSecondary}" TextTrimming="CharacterEllipsis"/></StackPanel>
      </CheckBox>
      <TextBlock Grid.Column="1" Text="{Binding Size}" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Right"/>
    </Grid>
  </DataTemplate>
</ResourceDictionary>
